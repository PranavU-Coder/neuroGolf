[project]
name = "neurogolf"
version = "0.1.2"
description = "competition for ARC-AGI public training or smthg idk"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
]
